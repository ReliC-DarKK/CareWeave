# =============================================================================
# CareWeave — Frontend Environment Configuration Template
# =============================================================================
#
# SECURITY NOTICE:
# Only variables prefixed with VITE_ are exposed to the client bundle.
# Never store secret keys, database passwords, private API keys, or JWT secrets
# in client .env files.
#
# Copy this file to `.env` or `.env.local` to customize local development settings.
# =============================================================================

# Backend API Base URL (P2 Backend Gateway)
VITE_API_BASE_URL=http://localhost:5000/api
