# Services Layer (Integration Stubs)

This directory is reserved for future integration with the backend and data services created by Person 2.

In this phase, the CareWeave frontend strictly consumes static mock data fixtures from `src/data/mockData.js`. No fake API infrastructure or network mocks have been implemented per architectural boundaries.
